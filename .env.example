# n8n webhook that performs the background removal
VITE_N8N_WEBHOOK_URL=YOUR_WEBHOOK_URL

# Optional: force the development fallback (mock result) even when a URL is set
VITE_ENABLE_DEMO_MODE=false

# Optional: max upload size in MB (default 10)
VITE_MAX_FILE_SIZE_MB=10
